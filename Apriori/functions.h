#include <string>
#include <iostream>
#include <fstream>

using namespace std;

int** buildDatabase(string); // delete the arrays that fill the array being returned and delete the final array that holds them all

void arrayDisplayer(int**);